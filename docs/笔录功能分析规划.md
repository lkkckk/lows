# 笔录分析功能规划

> 创建日期：2026-02-12  
> 最后更新：2026-02-12  
> 状态：规划阶段

---

## 一、MongoDB 版本评估结论

**结论：笔录分析功能不需要升级到 MongoDB 7.0。**

| 评估维度 | 说明 |
|---|---|
| 当前版本 | MongoDB 5.0（`docker-compose.yml` 中 `image: mongo:5.0`） |
| 向量搜索 | 当前方案为 Python 内存 NumPy 计算余弦相似度，对 <10K 级数据 <50ms 完成，完全够用 |
| MongoDB 7.0 `$vectorSearch` | 仅在数据量达 5 万+ 条文档时才有必要；社区版原生向量索引功能仍有限制 |
| 中文全文搜索 | 7.0 社区版**仍不支持中文分词**（需 Atlas Search Enterprise），当前系统已用 `$regex` + OpenSearch/ES 规避，升级不改变现状 |
| 笔录存储 | 纯文档存储，5.0 完全胜任 |
| 安全维护 | 5.0 于 2024.10 EOL；建议**后续从安全角度独立评估升级**，与本功能无关 |

---

## 二、核心设计理念

### 以案件为中心

所有操作都围绕**案件**展开。用户先创建案件，然后在案件内添加笔录、触发分析、查看结果。不存在"无案件的散落笔录"——每份笔录必须归属于一个案件。

```
用户操作流程：
新建案件 → 添加笔录（粘贴/上传）→ 单份分析 → 添加更多笔录 → 交叉分析 → 查看结果
```

### 笔录作为永久知识库

已分析的笔录**永久保存**，成为系统知识库的一部分：
- 笔录内容和分析结果长期留存，不因案件关闭而删除
- 笔录经过向量化后，可被 AI 问法模块语义检索引用（如"类似案件的处理方式"）
- 笔录中提取的关键事实和法条关联可被后续案件参考
- 支持全局搜索笔录内容（跨案件）

### 目标用户

基层办案民警，日常笔录分析和案件研判。

### 环境前提

- LLM：内网部署的 DeepSeek V3.1 或 Qwen 3 256B VL（OpenAI 兼容 API）
- 数据安全：全量内网，不存在泄露风险
- 基础设施：复用现有 MongoDB 5.0 + Embedding (bge-m3) + FastAPI 后端

---

## 三、功能规划

### 功能分两期

| 阶段 | 核心内容 | 预计工期 |
|---|---|---|
| **一期** | 案件管理 + 笔录 CRUD + 单份笔录 AI 分析 + 知识库沉淀 | 5~7 天 |
| **二期** | 同案多份笔录交叉分析 + 矛盾点比对 + 可视化 | 5~8 天 |

---

## 四、数据模型

### 4.1 `cases` 集合 — 案件

```json
{
  "case_id": "string",              // 唯一ID（UUID）
  "case_number": "string",          // 案件编号（用户填写，如 "北公治行受字[2026]001号"）
  "case_name": "string",            // 案件名称（如 "张某故意伤害案"）
  "case_type": "string",            // 案件类型：治安案件 / 刑事案件 / 行政案件
  "description": "string",          // 案件简述
  "tags": ["string"],               // 标签（如 "故意伤害"、"盗窃"）
  "transcript_count": 0,            // 笔录数量（冗余字段，便于列表展示）
  "cross_analysis": null,           // 交叉分析结果（二期填充，见下文）
  "created_at": "datetime",
  "updated_at": "datetime",
  "status": "string"                // active / archived
}
```

### 4.2 `transcripts` 集合 — 笔录

```json
{
  "transcript_id": "string",        // 唯一ID（UUID）
  "case_id": "string",              // 所属案件ID（必填，外键关联 cases）
  "title": "string",                // 笔录标题（如 "张某第一次询问笔录"）
  "type": "string",                 // 类型：询问笔录 / 讯问笔录 / 陈述笔录 / 辨认笔录
  "subject_name": "string",         // 被询问/讯问人姓名
  "subject_role": "string",         // 角色：嫌疑人 / 被害人 / 证人 / 报案人
  "content": "string",              // 笔录全文
  "file_name": "string",            // 原始文件名（上传时记录）
  "created_at": "datetime",
  "updated_at": "datetime",

  // --- AI 分析结果（分析完成后填充）---
  "analysis": {
    "persons": [
      {
        "name": "string",
        "role": "string",           // 嫌疑人 / 被害人 / 证人 / 询问人
        "id_number": "string",
        "contact": "string",
        "description": "string"     // 人物特征描述
      }
    ],
    "timeline": [
      {
        "time": "string",           // 时间描述
        "event": "string",          // 事件描述
        "source": "string"          // 原文引用
      }
    ],
    "locations": [
      {
        "name": "string",
        "type": "string",           // 事发地 / 报案地 / 询问地
        "detail": "string"
      }
    ],
    "key_facts": [
      {
        "description": "string",
        "category": "string",       // 行为 / 动机 / 后果 / 工具
        "source": "string"          // 原文引用
      }
    ],
    "items_amounts": [
      {
        "name": "string",
        "quantity": "string",
        "value": "string"
      }
    ],
    "related_laws": [
      {
        "law_title": "string",      // 法规名称
        "article_display": "string",// 如 "第五十八条"
        "law_id": "string",         // 法规ID（数据库关联）
        "article_num": "number",
        "relevance": "string",      // 适用说明
        "confidence": "string"      // high / medium / low
      }
    ],
    "compliance_issues": [
      {
        "item": "string",           // 检查项名称
        "status": "string",         // pass / warning / missing
        "detail": "string"          // 说明
      }
    ],
    "summary": "string"             // AI 分析摘要
  },

  // --- 知识库相关 ---
  "embedding": ["float"],           // 向量化（用于语义检索，分析后异步生成）
  "keywords": ["string"],           // 从分析中提取的关键词（用于全文检索）

  "analysis_status": "string"       // pending / analyzing / analyzed / failed
}
```

### 4.3 `cases.cross_analysis` — 交叉分析结果（二期）

```json
{
  "contradictions": [
    {
      "type": "string",            // 时间矛盾 / 事实矛盾 / 数量矛盾 / 细节矛盾
      "severity": "string",        // high / medium / low
      "description": "string",
      "sources": [
        {
          "transcript_id": "string",
          "person": "string",
          "quote": "string"        // 原文引用
        }
      ]
    }
  ],
  "unified_timeline": [
    {
      "time": "string",
      "event": "string",
      "agreed_by": ["string"],     // 一致的笔录ID
      "disputed_by": ["string"]    // 有异议的笔录ID
    }
  ],
  "evidence_chain": [
    {
      "type": "string",           // 言证 / 物证 / 书证 / 电子证据
      "description": "string",
      "status": "string",         // 已获取 / 待补充
      "source_transcripts": ["string"]
    }
  ],
  "consistency_score": "number",   // 整体一致性评分 0-100
  "summary": "string",
  "analyzed_at": "datetime"
}
```

---

## 五、前端页面设计

### 5.1 导航栏变更

在现有 4 个 tab 基础上新增第 5 个：

```
法规库 | 全文检索 | AI问法 | 文书模板 | 笔录分析（新增）
```

图标使用 `ClipboardList`（from lucide-react），与现有 `FileText`（文书模板）区分。

### 5.2 路由规划

```
/cases                       → CaseList        案件列表页（笔录分析 tab 入口）
/cases/create                → CaseCreate      新建案件页
/cases/:caseId               → CaseDetail      案件详情页（含笔录列表 + 交叉分析）
/cases/:caseId/transcripts/:id  → TranscriptDetail  笔录详情 + 分析结果页
/cases/search                → TranscriptSearch  笔录全局搜索页（知识库检索）
```

### 5.3 页面一：案件列表页（CaseList）

> 路由：`/cases`  
> 这是点击"笔录分析"tab 后的入口页面

```
┌─────────────────────────────────────────────────────────────────┐
│  [+ 新建案件]                            [🔍 搜索笔录知识库]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 📁 张某故意伤害案                                        │   │
│  │    案件编号：北公治行受字[2026]001号                      │   │
│  │    类型：治安案件  |  笔录：3份  |  状态：进行中          │   │
│  │    标签：[故意伤害] [轻微伤]                              │   │
│  │    创建时间：2026-02-10                                   │   │
│  │                                     [查看详情] [归档]     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 📁 李某盗窃案                                            │   │
│  │    案件编号：北公刑初字[2026]015号                        │   │
│  │    类型：刑事案件  |  笔录：5份  |  状态：进行中          │   │
│  │    标签：[盗窃] [入室]                                    │   │
│  │    创建时间：2026-02-08                                   │   │
│  │                                     [查看详情] [归档]     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 📁 王某某寻衅滋事案                          已归档 📦    │   │
│  │    ...                                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│                        [1] [2] [3] ...                           │
└─────────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 「+ 新建案件」按钮 → 跳转到 `/cases/create`
- 「🔍 搜索笔录知识库」按钮 → 跳转到 `/cases/search`（全局搜索所有案件的所有笔录）
- 案件卡片展示核心信息，点击「查看详情」进入案件内部
- 支持按案件类型、状态筛选；支持关键词搜索案件名称
- 「归档」操作将案件标记为 `archived`，不删除数据，仅在列表中降低优先级

### 5.4 页面二：新建案件页（CaseCreate）

> 路由：`/cases/create`

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回案件列表                                                 │
│                                                                 │
│  ┌──────────────────── 新建案件 ────────────────────────┐       │
│  │                                                       │       │
│  │  案件名称 *    [                                    ] │       │
│  │  案件编号      [                                    ] │       │
│  │  案件类型 *    [治安案件 ▼]                           │       │
│  │                 ○ 治安案件  ○ 刑事案件  ○ 行政案件    │       │
│  │  案件简述      [                                    ] │       │
│  │                [                                    ] │       │
│  │  标签          [+ 添加标签]  [故意伤害] [×]           │       │
│  │                                                       │       │
│  │               [取消]              [创建案件]          │       │
│  └───────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 必填项：案件名称、案件类型
- 可选项：案件编号（警综系统编号）、案件简述、标签
- 标签支持自由输入，常用标签可自动补全（如"故意伤害""盗窃""诈骗"等）
- 创建成功后自动跳转到案件详情页

### 5.5 页面三：案件详情页（CaseDetail）

> 路由：`/cases/:caseId`  
> 这是案件的核心工作页面，包含笔录管理和分析功能

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回        张某故意伤害案                                   │
│  案件编号：北公治行受字[2026]001号 | 治安案件 | 2026-02-10       │
│  简述：张某与邻居因噪音纠纷发生肢体冲突...                      │
│  标签：[故意伤害] [轻微伤] [邻里纠纷]                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─── 笔录列表 (3份) ────────────────────────────────────┐     │
│  │                                                        │     │
│  │  [+ 粘贴文本添加]  [📎 上传文件]                       │     │
│  │                                                        │     │
│  │  ┌─────────────────────────────────────────────────┐   │     │
│  │  │ 📄 张某第一次询问笔录                            │   │     │
│  │  │    被询问人：张某（嫌疑人）| 询问笔录             │   │     │
│  │  │    分析状态：✅ 已分析                            │   │     │
│  │  │    摘要：张某承认因噪音纠纷与李某发生口角...      │   │     │
│  │  │    关联法条：《治安管理处罚法》第四十三条         │   │     │
│  │  │                    [查看详情] [重新分析] [删除]    │   │     │
│  │  └─────────────────────────────────────────────────┘   │     │
│  │                                                        │     │
│  │  ┌─────────────────────────────────────────────────┐   │     │
│  │  │ 📄 李某陈述笔录                                  │   │     │
│  │  │    被询问人：李某（被害人）| 陈述笔录             │   │     │
│  │  │    分析状态：✅ 已分析                            │   │     │
│  │  │    摘要：李某称张某先动手推搡导致其摔倒...        │   │     │
│  │  │    关联法条：《治安管理处罚法》第四十三条         │   │     │
│  │  │                    [查看详情] [重新分析] [删除]    │   │     │
│  │  └─────────────────────────────────────────────────┘   │     │
│  │                                                        │     │
│  │  ┌─────────────────────────────────────────────────┐   │     │
│  │  │ 📄 王某证人证言                                  │   │     │
│  │  │    被询问人：王某（证人）| 询问笔录               │   │     │
│  │  │    分析状态：⏳ 分析中...                         │   │     │
│  │  │                    [查看详情]                      │   │     │
│  │  └─────────────────────────────────────────────────┘   │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                 │
│  ┌─── 交叉分析（二期功能）──────────────────────────────┐      │
│  │                                                       │      │
│  │  需要 2 份及以上已分析笔录                            │      │
│  │  当前：2/3 已分析                                     │      │
│  │                                                       │      │
│  │  [开始交叉分析]  （需全部笔录分析完成后可用）         │      │
│  │                                                       │      │
│  │  ─── 交叉分析结果 ───                                │      │
│  │  一致性评分：72 / 100                                 │      │
│  │                                                       │      │
│  │  ⚠ 矛盾点 (2处)：                                   │      │
│  │  🔴 时间矛盾：张某称 20:00 发生冲突，                │      │
│  │               李某称 19:30 已开始争吵                  │      │
│  │  🟡 细节矛盾：张某称推了一下，                       │      │
│  │               李某称被打了两拳                         │      │
│  │                                                       │      │
│  │  📋 统一时间线：                                      │      │
│  │  19:30 - 因噪音问题开始口角（李某、王某一致）        │      │
│  │  20:00 - 发生肢体冲突（各方存在分歧）                │      │
│  │  20:15 - 报警（张某、李某、王某一致）                │      │
│  │                                                       │      │
│  │  📦 证据链：                                          │      │
│  │  ✅ 言证：3 份笔录                                   │      │
│  │  ❓ 待补充：监控录像、伤情鉴定                       │      │
│  └───────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

**功能说明**：
- **顶部区域**：案件基本信息概览（只读，可点编辑按钮修改）
- **笔录列表区域**：
  - 「粘贴文本添加」→ 弹出 Modal，填写标题/类型/被询问人/角色/内容 → 提交后自动触发 AI 分析
  - 「上传文件」→ 支持 .docx / .txt，上传后自动解析内容，弹出确认框补充元信息 → 提交后触发分析
  - 每条笔录卡片展示：标题、被询问人及角色、分析状态、摘要（已分析时展示）、关联法条（已分析时展示）
  - 分析状态实时更新（前端轮询 `/api/cases/:caseId/transcripts/:id/status`）
  - 「查看详情」→ 进入笔录详情页
  - 「重新分析」→ 重新触发 AI 分析
  - 「删除」→ 二次确认后删除
- **交叉分析区域**（二期）：
  - 需 ≥2 份已分析笔录才能触发
  - 显示矛盾点列表（颜色区分严重程度）
  - 显示统一时间线
  - 显示证据链清单

### 5.6 页面四：笔录详情页（TranscriptDetail）

> 路由：`/cases/:caseId/transcripts/:id`  
> 左右分栏布局，左边看原文，右边看分析结果

```
┌──────────────────────────────────────────────────────────────────┐
│  ← 返回案件   张某第一次询问笔录   被询问人：张某（嫌疑人）      │
│                                            [重新分析] [导出]     │
├────────────────────────────┬─────────────────────────────────────┤
│                            │                                     │
│    ═══ 笔录原文 ═══       │    ═══ AI 分析结果 ═══              │
│                            │                                     │
│  问：你叫什么名字，身份   │  ┌─── 📋 分析摘要 ──────────────┐  │
│  证号是多少？              │  │ 张某（男，35岁）承认因邻居    │  │
│                            │  │ 噪音问题与李某发生口角，后    │  │
│  答：我叫张某，身份证号   │  │ 发展为肢体冲突。张某称仅推    │  │
│  45XXXX19910101XXXX。      │  │ 了李某一下，否认有殴打行为。  │  │
│                            │  └──────────────────────────────┘  │
│  问：请你详细叙述一下     │                                     │
│  事情的经过。              │  ┌─── 👤 涉及人员 ──────────────┐  │
│                            │  │                                │  │
│  答：2026年2月9日晚上     │  │  张某 — 嫌疑人               │  │
│  大约8点左右，我在家里    │  │    身份证：45XXXX199101...    │  │
│  看电视，楼上邻居李某     │  │    联系方式：138XXXX0001     │  │
│  家又开始大声放音乐。     │  │                                │  │
│  我上去敲门让他小声一     │  │  李某 — 被害人               │  │
│  点，结果他态度很恶劣     │  │    住址：XX小区X栋X单元      │  │
│  ，我们就吵了起来。       │  │                                │  │
│  后来他推了我一下，我     │  │  王某 — 证人                 │  │
│  就推了他一下，他自己     │  │    邻居，事发时在场           │  │
│  摔倒了。然后他老婆就     │  └──────────────────────────────┘  │
│  报了警。                  │                                     │
│                            │  ┌─── 🕐 时间线 ───────────────┐  │
│  问：你有没有打他？       │  │                                │  │
│                            │  │  20:00 楼上噪音开始           │  │
│  答：没有，我就推了一     │  │  20:10 张某上楼敲门交涉       │  │
│  下。                      │  │  20:15 双方发生口角           │  │
│  ...                       │  │  20:20 发生肢体冲突           │  │
│                            │  │  20:25 李某妻子报警           │  │
│  （可滚动阅读全文）       │  └──────────────────────────────┘  │
│                            │                                     │
│                            │  ┌─── 📍 地点 ─────────────────┐  │
│                            │  │  事发地：XX小区X栋楼道       │  │
│                            │  │  询问地：北流市公安局XX派出所 │  │
│                            │  └──────────────────────────────┘  │
│                            │                                     │
│                            │  ┌─── ⚖️ 关联法条 ──────────────┐  │
│                            │  │                                │  │
│                            │  │  《治安管理处罚法》第四十三条 │  │
│                            │  │  → 殴打他人 / 故意伤害        │  │
│                            │  │  适用置信度：高               │  │
│                            │  │  [点击查看法条详情]            │  │
│                            │  │                                │  │
│                            │  │  《治安管理处罚法》第九条     │  │
│                            │  │  → 因民间纠纷引起的治安案件   │  │
│                            │  │  适用置信度：中               │  │
│                            │  │  [点击查看法条详情]            │  │
│                            │  └──────────────────────────────┘  │
│                            │                                     │
│                            │  ┌─── ✅ 规范性检查 ────────────┐  │
│                            │  │                                │  │
│                            │  │  ✓ 已告知权利义务             │  │
│                            │  │  ✓ 询问人签名                 │  │
│                            │  │  ✓ 记录起止时间               │  │
│                            │  │  ⚠ 未发现见证人签名           │  │
│                            │  │  ✗ 缺少同步录音录像说明       │  │
│                            │  └──────────────────────────────┘  │
│                            │                                     │
└────────────────────────────┴─────────────────────────────────────┘
```

**功能说明**：
- **左栏（55%）**：笔录原文全文，纯文本展示，可滚动阅读
- **右栏（45%）**：AI 分析结果，按模块折叠/展开：
  - 📋 分析摘要：一段话概括核心内容
  - 👤 涉及人员：卡片列表（姓名、角色、关键信息）
  - 🕐 时间线：按时间排序的事件节点
  - 📍 地点：标签展示
  - ⚖️ 关联法条：每条显示法律名称+条号+适用说明+置信度，点击跳转 `/search?q=《xxx》第xx条`
  - ✅ 规范性检查：清单样式（✓ 通过 / ⚠ 警告 / ✗ 缺失）
- **顶部操作**：
  - 「← 返回案件」→ 回到案件详情页
  - 「重新分析」→ 重新触发 AI 分析
  - 「导出」→ 导出分析报告（Markdown / PDF，可选）
- 分析状态为 `analyzing` 时，右栏显示加载动画
- 分析失败时右栏显示错误信息和「重试」按钮

### 5.7 页面五：笔录知识库搜索页（TranscriptSearch）

> 路由：`/cases/search`  
> 跨案件全局搜索笔录内容，将历史笔录作为知识库使用

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回案件列表                                                 │
│                                                                 │
│  ┌──────── 笔录知识库检索 ──────────────────────────────┐      │
│  │  [请输入关键词搜索历史笔录...              ] [搜索]   │      │
│  │                                                       │      │
│  │  筛选：类型 [全部▼]  角色 [全部▼]  案件类型 [全部▼]  │      │
│  └───────────────────────────────────────────────────────┘      │
│                                                                 │
│  搜索结果：找到 12 条相关笔录                                   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 📄 赵某寻衅滋事案 — 赵某第二次讯问笔录                  │   │
│  │    被询问人：赵某（嫌疑人）| 讯问笔录                    │   │
│  │    匹配内容：...张某在酒后无故殴打路人，造成轻微伤...    │   │
│  │    关联法条：《刑法》第二百九十三条                       │   │
│  │    创建时间：2025-12-15                                   │   │
│  │                                    [查看笔录] [查看案件]  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 📄 刘某故意伤害案 — 被害人陈述笔录                      │   │
│  │    ...                                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│                        [1] [2] [3] ...                           │
└─────────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 搜索范围：所有案件中所有笔录的 `content`（全文）+ `analysis.summary`（摘要）+ `keywords`（关键词）
- 搜索方式：`$regex` 关键词匹配（与现有全文检索一致）+ 可选向量语义搜索
- 筛选器：按笔录类型、被询问人角色、案件类型筛选
- 搜索结果高亮匹配文本
- 每条结果可点击进入笔录详情或所属案件
- 用途："之前有没有类似案件的笔录可以参考？"

### 5.8 添加笔录 Modal 弹窗

> 在案件详情页点击「粘贴文本添加」或「上传文件」时弹出

**粘贴文本方式**：
```
┌────────────────── 添加笔录 ──────────────────────┐
│                                                    │
│  笔录标题 *    [张某第一次询问笔录              ] │
│  笔录类型 *    [询问笔录 ▼]                       │
│  被询问人 *    [张某                             ] │
│  角色 *        [嫌疑人 ▼]                          │
│                                                    │
│  笔录内容 *                                        │
│  ┌─────────────────────────────────────────────┐  │
│  │                                             │  │
│  │  （在此粘贴笔录全文）                       │  │
│  │                                             │  │
│  │                                             │  │
│  │                                             │  │
│  └─────────────────────────────────────────────┘  │
│                                                    │
│  ☑ 提交后自动触发 AI 分析                         │
│                                                    │
│              [取消]              [提交]             │
└────────────────────────────────────────────────────┘
```

**上传文件方式**：
```
┌────────────────── 上传笔录文件 ──────────────────┐
│                                                    │
│  ┌─────────────────────────────────────────────┐  │
│  │       📎 点击或拖拽文件到此处上传            │  │
│  │       支持 .docx / .txt，最大 10MB           │  │
│  └─────────────────────────────────────────────┘  │
│                                                    │
│  已选文件：张某询问笔录.docx  ✓                    │
│                                                    │
│  笔录标题 *    [张某询问笔录（自动填充）        ] │
│  笔录类型 *    [询问笔录 ▼]                       │
│  被询问人 *    [                                 ] │
│  角色 *        [嫌疑人 ▼]                          │
│                                                    │
│  ☑ 提交后自动触发 AI 分析                         │
│                                                    │
│              [取消]              [上传并提交]       │
└────────────────────────────────────────────────────┘
```

---

## 六、后端架构

### 6.1 新增文件

| 文件 | 职责 |
|---|---|
| `backend/app/api/cases.py` | 路由层：案件 CRUD + 笔录 CRUD + 分析触发 |
| `backend/app/services/case_service.py` | 业务层：案件管理 |
| `backend/app/services/transcript_service.py` | 业务层：笔录管理 + LLM 分析 + 法条关联 + 知识库沉淀 |

### 6.2 需修改的现有文件

| 文件 | 修改内容 |
|---|---|
| `backend/app/db.py` | 新增 `COLLECTION_CASES`、`COLLECTION_TRANSCRIPTS` 常量 |
| `backend/app/api/__init__.py` | 注册 `cases_router` |
| `backend/app/models/schemas.py` | 新增 `Case`、`Transcript`、`TranscriptAnalysis` 等 Pydantic 模型 |
| `mongodb/init-indexes.js` | 新增 `cases` 和 `transcripts` 集合的索引 |
| `frontend/src/App.jsx` | 新增路由 + 导航栏第 5 个 tab |
| `frontend/src/services/api.js` | 新增案件和笔录相关 API 调用函数 |

### 6.3 API 端点设计

#### 案件管理

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/cases` | 创建案件 |
| `GET` | `/api/cases` | 案件列表（分页，支持筛选/搜索） |
| `GET` | `/api/cases/{case_id}` | 获取案件详情（含笔录摘要列表） |
| `PUT` | `/api/cases/{case_id}` | 更新案件信息 |
| `PUT` | `/api/cases/{case_id}/archive` | 归档/取消归档案件 |
| `DELETE` | `/api/cases/{case_id}` | 删除案件（连带删除其下所有笔录，需管理员权限） |

#### 笔录管理（在案件下操作）

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/cases/{case_id}/transcripts` | 添加笔录（文本方式） |
| `POST` | `/api/cases/{case_id}/transcripts/upload` | 上传笔录文件（.docx / .txt） |
| `GET` | `/api/cases/{case_id}/transcripts` | 获取案件下所有笔录列表 |
| `GET` | `/api/cases/{case_id}/transcripts/{id}` | 获取笔录详情（含分析结果） |
| `DELETE` | `/api/cases/{case_id}/transcripts/{id}` | 删除笔录 |
| `POST` | `/api/cases/{case_id}/transcripts/{id}/analyze` | 触发/重新触发 AI 分析 |
| `GET` | `/api/cases/{case_id}/transcripts/{id}/status` | 查询分析状态 |

#### 交叉分析（二期）

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/cases/{case_id}/cross-analyze` | 触发交叉分析 |
| `GET` | `/api/cases/{case_id}/cross-analysis` | 获取交叉分析结果 |

#### 知识库搜索

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/cases/transcripts/search` | 全局搜索笔录知识库（跨案件） |

### 6.4 LLM 调用策略

**复用现有基础设施**：
- 复用 `ai_service.py` 中的 `_call_llm()` 函数和 `get_ai_config()` 配置
- 共享 LLM API 地址、密钥、模型配置

**独立的分析逻辑**：
- 独立系统提示词（笔录分析角色，非法律问答角色）
- 独立的 Function Calling 工具定义：
  - `extract_key_elements` — 结构化提取笔录要素（JSON 输出）
  - `search_related_laws` — 复用现有法条检索能力
  - `check_compliance` — 程序合规性检查
- `max_tokens` 调高到 4000~8000（笔录分析输出内容较多）
- 异步执行：通过 FastAPI `BackgroundTasks`，前端轮询状态

**上下文窗口**：DeepSeek V3.1 / Qwen 3 256B 均支持 128K+，单份笔录（5000~20000 字）无压力。

### 6.5 知识库沉淀流程

笔录分析完成后，自动执行以下知识库沉淀操作：

```
AI 分析完成
    ↓
1. 提取关键词 → 写入 transcript.keywords 字段
    ↓
2. 生成笔录向量 → 调用 embedding_client.get_embeddings()
    ↓                将 content 摘要向量化 → 写入 transcript.embedding
    ↓
3. 更新案件统计 → 更新 case.transcript_count
    ↓
笔录成为可检索的知识库内容
    - 关键词检索：$regex 匹配 content / keywords
    - 语义检索：向量相似度（复用现有内存余弦计算方式）
    - AI 问法可引用：扩展 search_for_rag 数据源（远期）
```

### 6.6 MongoDB 索引

```javascript
// ===== cases 集合 =====
db.cases.createIndex({ "case_id": 1 }, { unique: true, name: "idx_case_id" });
db.cases.createIndex({ "status": 1, "created_at": -1 }, { name: "idx_case_status_date" });
db.cases.createIndex({ "case_type": 1 }, { name: "idx_case_type" });
db.cases.createIndex({ "case_name": "text", "description": "text", "tags": "text" },
                      { name: "idx_case_text", default_language: "none" });

// ===== transcripts 集合 =====
db.transcripts.createIndex({ "transcript_id": 1 }, { unique: true, name: "idx_transcript_id" });
db.transcripts.createIndex({ "case_id": 1, "created_at": -1 }, { name: "idx_transcript_case_date" });
db.transcripts.createIndex({ "analysis_status": 1 }, { name: "idx_analysis_status" });
db.transcripts.createIndex({ "type": 1 }, { name: "idx_transcript_type" });
db.transcripts.createIndex({ "subject_role": 1 }, { name: "idx_subject_role" });
// 知识库全文检索（content + keywords）
db.transcripts.createIndex({ "content": "text", "keywords": "text" },
                           { name: "idx_transcript_text", default_language: "none" });
```

---

## 七、交叉分析（二期）

### 7.1 前提条件

- 案件下 ≥2 份笔录
- 所有笔录的 `analysis_status` 均为 `analyzed`

### 7.2 LLM 分步策略

```
第 1 步：各笔录独立分析（一期已完成）
    → 每份笔录有完整的 analysis（人员、时间线、关键事实）

第 2 步：拼接要素摘要 → 交叉比对
    → 将所有笔录的 analysis.summary + analysis.timeline + analysis.key_facts 拼接
    → 送入 LLM，指令：发现矛盾点、评估一致性
    → 输出：contradictions 列表 + consistency_score

第 3 步：定向深入（针对矛盾点）
    → 对每个矛盾点，从原始笔录中提取相关段落
    → 送入 LLM 做详细比对分析、严重程度评估
    → 输出：enriched contradictions（含原文引用）
```

每次 LLM 调用控制在 <20K tokens。

### 7.3 可视化组件（二期前端）

| 组件 | 说明 |
|---|---|
| 矛盾点列表 | 按严重程度排序，颜色区分（🔴 严重 / 🟡 中等 / 🔵 轻微） |
| 统一时间线 | 多人叙述的时间轴对齐，标注一致/有异议的节点 |
| 证据链清单 | 已获取 ✅ / 待补充 ❓ 分类展示 |
| 一致性仪表盘 | 环形图展示整体一致性评分（0-100） |

---

## 八、实施步骤

### 一期（案件管理 + 笔录分析 + 知识库）— 预计 5~7 天

| 序号 | 步骤 | 涉及文件 | 天数 |
|---|---|---|---|
| 1 | 数据模型定义（Pydantic 模型 + 集合常量） | `schemas.py`、`db.py` | 0.5 |
| 2 | 案件 CRUD 服务层 | 新建 `services/case_service.py` | 0.5 |
| 3 | 笔录 CRUD + 文件上传解析 | 新建 `services/transcript_service.py` | 1 |
| 4 | 笔录 AI 分析（LLM 提示词 + Function Calling） | `transcript_service.py` | 1 |
| 5 | 知识库沉淀（关键词提取 + 向量化 + 搜索） | `transcript_service.py` | 0.5 |
| 6 | API 路由层 | 新建 `api/cases.py`，修改 `api/__init__.py` | 0.5 |
| 7 | 前端：案件列表页 + 新建案件页 | 新建 `CaseList.jsx`、`CaseCreate.jsx` | 0.5 |
| 8 | 前端：案件详情页（笔录管理 + 添加弹窗） | 新建 `CaseDetail.jsx` | 1 |
| 9 | 前端：笔录详情页（左右分栏） | 新建 `TranscriptDetail.jsx` | 0.5 |
| 10 | 前端：笔录知识库搜索页 | 新建 `TranscriptSearch.jsx` | 0.5 |
| 11 | 路由注册 + 导航栏 + API 函数 | `App.jsx`、`api.js` | 0.5 |
| 12 | MongoDB 索引 + 测试调优 | `init-indexes.js` + 联调 | 1 |

### 二期（交叉分析）— 预计 5~8 天

| 序号 | 步骤 | 天数 |
|---|---|---|
| 1 | 交叉分析 LLM 策略（摘要→比对→深入） | 2 |
| 2 | 交叉分析 API 端点 | 0.5 |
| 3 | 前端：案件详情页交叉分析区域 | 1.5 |
| 4 | 矛盾点可视化 + 时间线对比视图 | 2 |
| 5 | 证据链清单 + 一致性评分 | 1 |
| 6 | 测试与调优 | 1 |

---

## 九、新增文件清单

### 后端

| 文件 | 职责 |
|---|---|
| `backend/app/api/cases.py` | 案件 + 笔录路由 |
| `backend/app/services/case_service.py` | 案件业务逻辑 |
| `backend/app/services/transcript_service.py` | 笔录 CRUD + AI 分析 + 知识库沉淀 |

### 前端

| 文件 | 职责 |
|---|---|
| `frontend/src/pages/CaseList.jsx` | 案件列表页（tab 入口） |
| `frontend/src/pages/CaseCreate.jsx` | 新建案件页 |
| `frontend/src/pages/CaseDetail.jsx` | 案件详情页（笔录管理 + 交叉分析） |
| `frontend/src/pages/TranscriptDetail.jsx` | 笔录详情页（左右分栏） |
| `frontend/src/pages/TranscriptSearch.jsx` | 笔录知识库搜索页 |
| `frontend/src/styles/Case.css` | 案件相关样式 |
| `frontend/src/styles/Transcript.css` | 笔录相关样式 |

---

## 十、技术风险与应对

| 风险 | 影响 | 应对方案 |
|---|---|---|
| 笔录过长（>20K 字） | LLM token 溢出 | DeepSeek V3.1 / Qwen 3 均支持 128K 上下文，风险低；超长可分段摘要 |
| LLM 要素提取不稳定 | 结果格式不一致 | JSON Schema 约束 + Function Calling 结构化返回 + 失败自动重试 |
| 敏感信息展示 | 笔录含个人信息 | 页面加管理员鉴权（复用 `X-Admin-Token`） |
| 分析耗时长（>30s） | 用户等待 | `BackgroundTasks` 异步 + 前端轮询状态 + 加载动画 |
| 文件上传安全 | 恶意文件 | 限制 `.docx`/`.txt`，最大 10MB，校验 MIME |
| DOCX 解析 | 新增依赖 | `python-docx` 添加到 `requirements.txt` |
| 知识库数据膨胀 | 向量检索变慢 | 当前内存方案对 <10K 笔录足够；超过后可切换到 OpenSearch |

---

## 十一、与现有系统的集成关系

### 复用（不修改逻辑）

| 模块 | 复用方式 |
|---|---|
| `ai_service._call_llm()` | 直接调用，共享 LLM 配置 |
| `ai_service.get_ai_config()` | 读取同一套 AI 配置 |
| `law_service.search_for_rag()` | 法条关联检索 |
| `embedding_client.get_embeddings()` | 笔录向量化 |
| `X-Admin-Token` 鉴权 | 删除操作权限控制 |
| `/search?q=` 链接机制 | 法条跳转 |

### 不受影响

| 模块 | 说明 |
|---|---|
| 法规库（laws） | 独立集合，不交叉 |
| AI 问法（ai/chat） | 独立对话逻辑（远期可扩展数据源） |
| 文书模板（templates） | 独立功能 |
| QA 记忆库（qa_memory） | 仅服务于 AI 问法 |
| Docker 服务架构 | 不新增容器 |
| MongoDB 版本 | 保持 5.0 |

---

## 十二、依赖清单

### 新增 Python 依赖

| 包 | 用途 | 添加到 |
|---|---|---|
| `python-docx` | 解析 DOCX 文件内容 | `backend/requirements.txt` |

### 新增前端依赖

无需新增。现有 Ant Design + Lucide React + React Router 已满足所有 UI 需求。

AI 问法集成 — 将笔录知识库接入 search_for_rag 数据源，让 AI 对话能引用历史笔录
导出功能 — 交叉分析报告导出为 PDF/Word
超长笔录分段摘要 — 对 >20K 字的笔录做分段分析再合并
语义检索增强 — 用向量搜索替代 $regex 做笔录知识库检索
笔录模板关联 — 将分析结果自动填充到文书模板
如果你需要继续推进三期，我可以先出一份规划再实施，或者你直接告诉我想做哪些功能。