import React, { useState } from 'react';
import { 
  Search, 
  Book, 
  FileText, 
  Clock, 
  ChevronRight, 
  Shield, 
  Menu, 
  Filter, 
  Star,
  Zap
} from 'lucide-react';

// --- 模拟数据源 (Mock Data) ---
const MOCK_LAWS = [
  { 
    id: 1, 
    title: "中华人民共和国治安管理处罚法", 
    department: "全国人大常委会", 
    date: "2012-10-26", 
    status: "有效",
    category: "行政",
    tags: ["常用", "派出所必备"],
    summary: "维护社会治安秩序，保障公共安全，保护公民、法人和其他组织的合法权益。"
  },
  { 
    id: 2, 
    title: "中华人民共和国刑法", 
    department: "全国人大", 
    date: "2020-12-26", 
    status: "有效",
    category: "刑事",
    tags: ["重案", "基础法"],
    summary: "规定犯罪和刑罚的法律，是打击犯罪、保护人民的有力武器。"
  },
  { 
    id: 3, 
    title: "公安机关办理行政案件程序规定", 
    department: "公安部", 
    date: "2020-08-01", 
    status: "有效",
    category: "程序",
    tags: ["办案规范", "程序法"],
    summary: "规范公安机关行政办案程序，保障公安机关在行政执法中严格规范公正文明执法。"
  },
  { 
    id: 4, 
    title: "中华人民共和国人民警察法", 
    department: "全国人大常委会", 
    date: "2012-10-26", 
    status: "有效",
    category: "行政",
    tags: ["职权", "义务"],
    summary: "维护国家安全，维护社会治安秩序，保护公民合法权益。"
  },
  { 
    id: 5, 
    title: "中华人民共和国反有组织犯罪法", 
    department: "全国人大常委会", 
    date: "2021-12-24", 
    status: "有效",
    category: "刑事",
    tags: ["扫黑除恶"],
    summary: "预防和惩治有组织犯罪，加强和规范反有组织犯罪工作。"
  }
];

const RECENT_SEARCHES = ["传唤时间", "赌博 罚款", "袭警罪", "取保候审"];

// --- 组件定义 ---

// 1. 顶部导航栏
const Navbar = ({ activeTab, onTabChange }) => (
  <header className="bg-slate-900 text-white shadow-lg sticky top-0 z-50">
    <div className="container mx-auto px-4 h-16 flex items-center justify-between">
      {/* Logo 区 */}
      <div className="flex items-center gap-3">
        <div className="bg-blue-600 p-1.5 rounded-lg">
          <Shield size={24} className="text-white" />
        </div>
        <div>
          <h1 className="text-lg font-bold tracking-wide">警务法律离线查询终端</h1>
          <p className="text-[10px] text-slate-400 -mt-1 tracking-wider">POLICE LAW KNOWLEDGE BASE</p>
        </div>
      </div>

      {/* 导航菜单 */}
      <nav className="hidden md:flex items-center gap-1 bg-slate-800 p-1 rounded-lg">
        {[
          { id: 'laws', label: '法规库', icon: Book },
          { id: 'search', label: '全文检索', icon: Search },
          { id: 'templates', label: '文书模板', icon: FileText },
        ].map((item) => (
          <button
            key={item.id}
            onClick={() => onTabChange(item.id)}
            className={`
              flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-all
              ${activeTab === item.id 
                ? 'bg-blue-600 text-white shadow-sm' 
                : 'text-slate-400 hover:text-white hover:bg-slate-700'}
            `}
          >
            <item.icon size={16} />
            {item.label}
          </button>
        ))}
      </nav>

      {/* 用户/系统状态 */}
      <div className="flex items-center gap-4">
        <div className="hidden lg:flex items-center gap-2 px-3 py-1 bg-green-500/10 border border-green-500/20 rounded-full">
          <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
          <span className="text-xs text-green-400 font-medium">离线服务运行中</span>
        </div>
        <div className="w-8 h-8 rounded-full bg-slate-700 flex items-center justify-center text-xs font-bold border border-slate-600">
          警
        </div>
      </div>
    </div>
  </header>
);

// 2. 法规列表卡片
const LawCard = ({ law }) => (
  <div className="group bg-white border border-slate-200 rounded-xl p-5 hover:shadow-md hover:border-blue-300 transition-all cursor-pointer relative overflow-hidden">
    {/* 装饰背景 */}
    <div className="absolute top-0 right-0 p-4 opacity-5 group-hover:opacity-10 transition-opacity">
      <Shield size={64} className="text-blue-600 transform rotate-12 translate-x-4 -translate-y-4" />
    </div>

    <div className="relative z-10">
      <div className="flex justify-between items-start mb-2">
        <div className="flex gap-2 mb-2">
          <span className={`px-2 py-0.5 rounded text-xs font-medium border
            ${law.category === '刑事' ? 'bg-red-50 text-red-600 border-red-100' : 
              law.category === '行政' ? 'bg-blue-50 text-blue-600 border-blue-100' :
              'bg-slate-100 text-slate-600 border-slate-200'}
          `}>
            {law.category}
          </span>
          {law.status === '有效' && (
            <span className="px-2 py-0.5 rounded text-xs font-medium bg-green-50 text-green-600 border border-green-100">
              现行有效
            </span>
          )}
        </div>
        <button className="text-slate-400 hover:text-yellow-500 transition-colors">
          <Star size={18} />
        </button>
      </div>

      <h3 className="text-lg font-bold text-slate-800 mb-2 group-hover:text-blue-700 transition-colors">
        {law.title}
      </h3>
      
      <p className="text-sm text-slate-500 mb-4 line-clamp-2 leading-relaxed">
        {law.summary}
      </p>

      <div className="flex items-center justify-between text-xs text-slate-400 pt-4 border-t border-slate-100">
        <div className="flex gap-3">
          <span>{law.department}</span>
          <span>•</span>
          <span>{law.date} 实施</span>
        </div>
        <div className="flex items-center gap-1 text-blue-600 font-medium opacity-0 group-hover:opacity-100 transition-opacity">
          查阅详情 <ChevronRight size={14} />
        </div>
      </div>
    </div>
  </div>
);

// 3. 首页视图 (Laws Tab)
const HomeView = () => {
  const [filterText, setFilterText] = useState('');
  const [activeCategory, setActiveCategory] = useState('全部');

  const categories = ['全部', '常用', '刑事', '行政', '程序', '交通'];

  const filteredLaws = MOCK_LAWS.filter(law => {
    const matchesText = law.title.includes(filterText) || law.summary.includes(filterText);
    const matchesCategory = activeCategory === '全部' || law.category === activeCategory || law.tags.includes(activeCategory);
    return matchesText && matchesCategory;
  });

  return (
    <div className="pb-12">
      {/* 搜索 Hero 区域 */}
      <div className="bg-gradient-to-b from-slate-900 to-slate-800 pt-8 pb-16 px-4">
        <div className="container mx-auto max-w-4xl">
          <h2 className="text-2xl md:text-3xl font-bold text-white text-center mb-8">
            精准执法，有法可依
          </h2>
          
          <div className="relative">
            <div className="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
              <Search className="h-5 w-5 text-slate-400" />
            </div>
            <input
              type="text"
              className="block w-full pl-12 pr-4 py-4 rounded-xl border-0 ring-1 ring-slate-600 bg-slate-800/50 text-white placeholder:text-slate-400 focus:ring-2 focus:ring-blue-500 focus:bg-slate-800 transition-all shadow-xl text-lg"
              placeholder="搜索法规名称，如 '治安管理处罚法'..."
              value={filterText}
              onChange={(e) => setFilterText(e.target.value)}
            />
            <div className="absolute inset-y-0 right-2 flex items-center">
              <button className="bg-blue-600 hover:bg-blue-500 text-white px-4 py-1.5 rounded-lg text-sm font-medium transition-colors">
                搜索
              </button>
            </div>
          </div>

          {/* 快速标签 */}
          <div className="flex flex-wrap items-center justify-center gap-2 mt-6 text-sm">
            <span className="text-slate-400 flex items-center gap-1"><Clock size={14}/> 最近搜索：</span>
            {RECENT_SEARCHES.map((term, index) => (
              <button key={index} className="bg-slate-700/50 hover:bg-slate-700 text-slate-300 px-3 py-1 rounded-full transition-colors">
                {term}
              </button>
            ))}
          </div>
        </div>
      </div>

      {/* 主内容区：分类与列表 */}
      <div className="container mx-auto px-4 -mt-8 relative z-10">
        <div className="bg-white rounded-xl shadow-lg border border-slate-100 min-h-[500px] flex flex-col md:flex-row overflow-hidden">
          
          {/* 左侧：分类过滤器 */}
          <div className="w-full md:w-48 bg-slate-50 border-b md:border-b-0 md:border-r border-slate-100 p-4">
            <div className="flex items-center gap-2 text-slate-800 font-bold mb-4 px-2">
              <Filter size={18} />
              <span>法规分类</span>
            </div>
            <div className="flex flex-row md:flex-col gap-1 overflow-x-auto md:overflow-visible pb-2 md:pb-0 scrollbar-hide">
              {categories.map(cat => (
                <button
                  key={cat}
                  onClick={() => setActiveCategory(cat)}
                  className={`
                    px-4 py-2.5 rounded-lg text-sm text-left font-medium transition-all whitespace-nowrap
                    ${activeCategory === cat 
                      ? 'bg-blue-600 text-white shadow-md shadow-blue-500/20' 
                      : 'text-slate-600 hover:bg-white hover:text-blue-600'}
                  `}
                >
                  {cat}
                </button>
              ))}
            </div>
            
            {/* 快速工具入口 */}
            <div className="mt-8 hidden md:block">
              <div className="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-3 px-2">常用工具</div>
              <button className="w-full flex items-center gap-2 px-3 py-2 text-sm text-slate-600 hover:bg-white rounded-lg transition-colors">
                <Zap size={16} className="text-yellow-500" />
                <span>法条速查</span>
              </button>
              <button className="w-full flex items-center gap-2 px-3 py-2 text-sm text-slate-600 hover:bg-white rounded-lg transition-colors">
                <Star size={16} className="text-blue-500" />
                <span>我的收藏</span>
              </button>
            </div>
          </div>

          {/* 右侧：法规列表 */}
          <div className="flex-1 p-6 bg-slate-50/30">
            <div className="flex justify-between items-center mb-4">
              <h3 className="font-bold text-slate-800 text-lg">
                {activeCategory === '全部' ? '全部法规' : `${activeCategory}类法规`}
                <span className="text-slate-400 text-sm font-normal ml-2">({filteredLaws.length})</span>
              </h3>
              <div className="text-sm text-slate-500">按发布日期排序</div>
            </div>

            <div className="grid grid-cols-1 xl:grid-cols-2 gap-4">
              {filteredLaws.map(law => (
                <LawCard key={law.id} law={law} />
              ))}
            </div>
            
            {filteredLaws.length === 0 && (
              <div className="flex flex-col items-center justify-center py-20 text-slate-400">
                <Book size={48} className="mb-4 opacity-20" />
                <p>未找到相关法规</p>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
};

// 4. 其他 Tab 的占位符
const PlaceholderView = ({ title, icon: Icon, desc }) => (
  <div className="container mx-auto px-4 py-12 flex flex-col items-center justify-center text-center min-h-[60vh]">
    <div className="w-20 h-20 bg-slate-100 rounded-full flex items-center justify-center mb-6">
      <Icon size={32} className="text-slate-400" />
    </div>
    <h2 className="text-2xl font-bold text-slate-800 mb-2">{title}</h2>
    <p className="text-slate-500 max-w-md">{desc}</p>
  </div>
);

// --- 主应用入口 ---
export default function App() {
  const [activeTab, setActiveTab] = useState('laws');

  return (
    <div className="min-h-screen bg-slate-100 font-sans text-slate-900 selection:bg-blue-100 selection:text-blue-900">
      <Navbar activeTab={activeTab} onTabChange={setActiveTab} />
      
      <main>
        {activeTab === 'laws' && <HomeView />}
        
        {activeTab === 'search' && (
          <PlaceholderView 
            title="全库全文检索" 
            icon={Search} 
            desc="支持对所有已收录的法律法规进行全文检索，支持多关键词组合，搜索结果精确到具体的条文。" 
          />
        )}
        
        {activeTab === 'templates' && (
          <PlaceholderView 
            title="法律文书生成" 
            icon={FileText} 
            desc="选择文书模板，填写案件关键要素，自动生成标准规范的法律文书 PDF 文件。" 
          />
        )}
      </main>

      {/* Footer */}
      <footer className="bg-slate-900 text-slate-500 py-8 mt-auto">
        <div className="container mx-auto px-4 text-center text-sm">
          <p className="mb-2">&copy; 2025 警务法律离线查询系统 | 内网专用版本 V1.0</p>
          <p className="text-slate-600">本系统数据来源于公开渠道，仅供执法参考，请以正式文本为准</p>
        </div>
      </footer>
    </div>
  );
}